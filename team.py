# 평균을 구하는 함수
def f_avg(data): # 최준호 수정
    result = sum(data)/len(sum)
    return result

# 합계를 구하는 함수
def f_sum(data):
    # 코드를 입력해주세요
    return 1

# 오름차순으로 정렬해주는 함수
def f_sort(data) :
    return data.sort()

# 평균구하기